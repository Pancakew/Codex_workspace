# verify scripts

验证脚本，不做正式训练。

- `run_verify_cmdvel.py`：验证底盘命令到关节目标再到小车运动。
- `run_verify_env.py`：验证最小 DirectRLEnv reset/step。
- `demo_fwmini_import_and_control.py`：导入 FW-mini 并跑控制 demo。

如果机器人不动、轮子顺序不对、转向角异常，先看这里。

