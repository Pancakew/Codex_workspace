[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crowd_nav_rl"
version = "0.1.0"
description = "Dense crowd navigation RL environment."
dependencies = ["isaaclab", "isaaclab-arena"]